# name: npm test
# on: [push]
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         node_version: [18.x]
#     steps:
#       - uses: actions/checkout@v4
#       - name: Use Node.js $
#         uses: actions/setup-node@v4
#         with:
#           node-version: $
#       - run: npm install
#       - run: npm run build --if-present
#       - run: npm test
name: Node.js CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]  # Test multiple Node versions
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'  # Enable dependency caching
    
    - name: Install dependencies
      run: npm ci  # Strict install using lockfile
      
    - name: Run build step
      run: npm run build --if-present
      
    - name: Run tests
      run: npm test
